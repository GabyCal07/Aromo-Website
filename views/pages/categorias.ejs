<!DOCTYPE html>
<html lang="es">
<head>
    <%- include('../partials/meta') %>    
</head>    

<body>
    <header>
        <%- include('../partials/navbar') %> 
    </header>
    
    <main class="bg-light-gray py-4">
        
        <div class="container">           
                        
            <div class="row">
                <div class="bg-light-green text-center mb-5">                        
                    <h1 class="section-title ps-3 pt-4"> Estos son los rubros más buscados por los vecinos del Barrio.</h1>
                    <h2 class="section-subtitle ps-3 pb-4"> Podés elegir el que se ajuste a tus necesidades.</h2>
                </div>
                <% if (JSONBody.length === 0){ %>                     
                    <h2 class="section-subtitle ps-3 pb-4"> No se encontró ningun rubro</h2>
                <% } %>                

                <% JSONBody.forEach(cat =>{ %>                    
                    <div class="col-xl-3 col-md-6 col-sm-12 col-mant">                        
                        <a href="/<%= template %>/<%= cat.name.toLowerCase() %>" class="card-link">
                        <div class="card my-3">                            
                            
                            <div class="card-body card-style">                                
                                <img class="card-image" src="<%= cat.img %>" alt="<%= cat.name %>"/>
                                <h5 class="card-title card-title-style pt-3"><%= cat.name %></h5>                                
                            </div>
                        </div>    
                        </a>
                    </div> 
                <% }) %>    
            </div>
        </div>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>